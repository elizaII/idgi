<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".android.activities.BrowseUsersActivity">

        <include
            layout="@layout/app_bar"/>

        <Button
            style="@style/menu_button"
            android:text="@string/browse_users_btn_name"
            android:id="@+id/browse_users_btn_name"
            android:onClick="onNameButtonClick" />

        <Button
            style="@style/menu_button"
            android:text="@string/browse_users_btn_course"
            android:id="@+id/browse_users_btn_course"
            android:onClick="onCourseButtonClick" />

    </LinearLayout>

    <include
        layout="@layout/nav_drawer"/>

</android.support.v4.widget.DrawerLayout>
